# Response Language

Respond in the language the user writes in. Match it for every reply in the conversation, not just the first one.

This rule covers conversational responses only. Code, comments, and documentation follow a separate, fixed rule — see [Code Language](code-language.md).
