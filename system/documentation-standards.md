# What `system/` Is For

This directory holds project-wide rules for AI agents working in this repository. One topic per file. `AGENTS.md` links to every file here — it does not restate their content. A restated rule is a copy that goes stale; a link cannot drift.

When adding a new rule: create one file per topic in `system/`, then reference it from `AGENTS.md`.
